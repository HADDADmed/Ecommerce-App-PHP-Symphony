
{% extends 'base.html.twig' %}


     {% block header %}
          {% include 'Partials/_header1.html.twig' %}
     {% endblock %}
{% block body %}
 
                    <div class="d-flex justify-content-center">   
                 <div class="container mt-4">
                 <div class="row" >
                    <div class="col-12 col-sm-8 col-md-6 col-lg-4 mx-auto">
                    <div class="card">
                            <div class="card" style="width: 18rem;">
                        <img class="card-img" src="{{ asset(product.imgPath)}}" alt="{{product.name}}">
                        <div class="card-body">
                            <h5 class="card-title">{{product.name}}</h5>
                                                    <a href="{{path('category_d',{ 'id': product.id })}}">  {{product.category.name}} </a>
                            <p class="card-text">{{product.description}}  </p>
                          <div class="price text-success text-center"><h5 class="mt-4">${{product.price}}</h5></div>          
                                <a class="btn btn-danger btn-sm d-flex justify-content-center"  onclick="return confirm('are u sure?')" href="{{path('product_delete',{ 'id': product.id })}}"type="button">Suppression</a>
                                <a class="btn btn btn-secondary btn-sm mt-2 d-flex justify-content-center" href="{{path('product_edit',{ 'id': product.id })}}" type="button">Edit</a></div>                        </div>
                         </div>
                  </div>
                    </div>
                    
                {# </div> #}

                {# <div class="row" >
                    <div class="col-12 col-sm-8 col-md-6 col-lg-4 mx-auto">
                    <div class="card">
                        <img class="card-img" src="{{ asset(product.imgPath)}}" alt="{{product.name}}">
                        <div class="card-img-overlay d-flex justify-content-end">
                        </div>
                        <div class="card">
                        <h4 class="card-title">{{product.name}}</h4>
                        <a href="{{path('category_d',{ 'id': product.id })}}">  {{product.category.name}} </a>
                        <p class="card-text">
                                    {{product.description}}           </p>
                        <div class=" text-center">
                         <div class="price text-success text-center"><h5 class="mt-4">${{product.price}}</h5></div>          
                        </div>
                         <div class="d-flex flex-column mt-4">
                                <a class="btn btn-danger btn-sm"  onclick="return confirm('are u sure?')" href="{{path('product_delete',{ 'id': product.id })}}"type="button">Suppression</a>
                                <a class="btn btn btn-secondary btn-sm mt-2" href="{{path('product_edit',{ 'id': product.id })}}" type="button">Edit</a></div>
                        </div>
                        </div>
                        <div class="d-flex flex-column mt-4">
                                <a class="btn btn-danger btn-sm"  onclick="return confirm('are u sure?')" href="{{path('product_delete',{ 'id': product.id })}}"type="button">Suppression</a>
                                <a class="btn btn btn-secondary btn-sm mt-2" href="{{path('product_edit',{ 'id': product.id })}}" type="button">Edit</a></div>
                        </div>
                    </div>
                    </div>
                    
                </div>
                 #}
                </div>

                </div>

{% endblock %}